/* import React from 'react';
import TimeStamp from '../../../../components/_helper_components/article/timestamp';


const SliderItem = ({data}) => {

  const { image, label, headline, timestamp } = data;

    return (
        <div></div>
    )
};

export default SliderItem; */
